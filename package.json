{
  "name": "githubpages",
  "version": "1.0.0",
  "description": "A place to hold my portfolio",
  "main": "index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild --watch --bundle --sourcemap --target=es6 --outfile=index.js client/app.jsx"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "esbuild": "^0.19.11",
    "react": "^18.2.0",
    "vite": "^5.0.11"
  }
}
